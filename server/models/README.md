# Models Directory

Place your trained YOLOv8 model file (`best.pt`) here after training on Google Colab.

The server will automatically load `best.pt` if it exists, otherwise falls back to pretrained `yolov8n.pt`.
